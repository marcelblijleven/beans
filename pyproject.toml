[tool]
[tool.commitizen]
name = "cz_conventional_commits"
version = "0.2.0"
tag_format = "v$version"
change_type_map = { feat = "✨ Features", fix = "🐛 Bug Fixes", refactor = "🔨 Refactor", perf = "⚡️ Performance", ci = "🛠 CI", test = "🚦 Tests" }